{
  "name": "sabir",
  "version": "0.1.0",
  "private": true,
  "description": "Local Arabic tutor — Quranic comprehension via pattern recognition",
  "scripts": {
    "build:client": "tsc public/app.ts --outDir public --rootDir public --target ES2020 --module ESNext --moduleResolution bundler --strict --lib ES2020,DOM --skipLibCheck --ignoreConfig",
    "start": "npm run build:client && tsx server.ts",
    "dev": "tsx --watch server.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3"
  }
}
